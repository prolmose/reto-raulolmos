import {  Fonts, Colors } from 'styles/';

const BigDoubleTextStyle = {
	highText: {
		fontSize: Fonts.size.huge,
		color: Colors.white,
		fontWeight: Fonts.weight.bold,
		margin: 0,
		padding: 0
	},
	lowText: {
		fontSize: Fonts.size.title,
		fontWeight: 'lighter',
		color: Colors.white,
		marginHorizontal: 0,
		marginVertical: 10,
		padding: 0,
	},
	box: {
		width: '400px'
	}
};

export default BigDoubleTextStyle;