import {  Fonts, Colors } from 'styles/';

const DoubleTextStyle = {
	title: {
		fontSize: Fonts.size.title,
		color: Colors.dark,
		weight: Fonts.weight.bold	
	},
	body: {
		fontSize: Fonts.size.body,
		color: Colors.subdued,
	}
};

export default DoubleTextStyle;